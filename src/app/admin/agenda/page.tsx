import{requirePermission}from"@/lib/access";import{getAdminSlots}from"@/lib/admin-data";import{prisma}from"@/lib/prisma";import{PageHeader}from"@/components/layout/page-header";import{SlotManager}from"@/components/admin/slot-manager";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{dateTime}from"@/lib/utils";import{StatusBadge}from"@/components/layout/status-badge";
export default async function Page(){await requirePermission("orders.view");const slots=await getAdminSlots();const upcoming=prisma?await prisma.order.findMany({where:{collectionAt:{gte:new Date()},status:{not:"CANCELED"}},take:12,orderBy:{collectionAt:"asc"},include:{customer:{select:{name:true}}}}):[];return <div className="page-container"><PageHeader title="Agenda e horários" description="Capacidade de atendimento e próximas coletas."/><div className="grid gap-5 xl:grid-cols-[.9fr_1.1fr]"><div><SlotManager slots={slots as any}/></div><Card><CardHeader><CardTitle>Próximas coletas</CardTitle></CardHeader><CardContent className="divide-y p-0">{upcoming.length?upcoming.map((o:any)=><div key={o.id} className="flex items-center justify-between gap-3 px-5 py-3"><div><strong className="text-sm">{o.customer.name}</strong><p className="mt-1 text-xs text-slate-500">{o.number} · {dateTime(o.collectionAt)}</p></div><StatusBadge status={o.status}/></div>):<p className="p-5 text-sm text-slate-500">Nenhuma coleta futura no banco.</p>}</CardContent></Card></div></div>}
