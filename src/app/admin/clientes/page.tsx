import{requirePermission}from"@/lib/access";import{getAdminCustomers}from"@/lib/admin-data";import{PageHeader}from"@/components/layout/page-header";import{Card}from"@/components/ui/card";import{Badge}from"@/components/ui/badge";import{CustomerActions}from"@/components/admin/customer-actions";import{dateTime}from"@/lib/utils";
export default async function Page(){await requirePermission("customers.view");const customers=await getAdminCustomers();return <div className="page-container"><PageHeader title="Clientes" description="Cadastros, contatos e volume de pedidos."/><Card className="divide-y">{customers.map((c:any)=><div key={c.id} className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center"><div className="flex-1"><div className="flex items-center gap-2"><strong>{c.name}</strong><Badge variant={c.status==="DISABLED"?"secondary":"success"}>{c.status==="DISABLED"?"Desativado":"Ativo"}</Badge></div><p className="mt-1 text-xs text-slate-500">{c.email} · {c.phone??"sem telefone"}</p><p className="mt-1 text-[10px] text-slate-400">Cliente desde {dateTime(c.createdAt)} · {c._count?.customerOrders??0} pedido(s)</p></div><CustomerActions id={c.id} status={c.status??"ACTIVE"}/></div>)}</Card></div>}
