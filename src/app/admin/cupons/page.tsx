import{requirePermission}from"@/lib/access";import{getAdminCoupons}from"@/lib/admin-data";import{PageHeader}from"@/components/layout/page-header";import{CouponManager}from"@/components/admin/coupon-manager";
export default async function Page(){await requirePermission("settings.manage");const coupons=await getAdminCoupons();return <div className="page-container"><PageHeader title="Cupons de desconto" description="Crie campanhas e acompanhe a utilização dos cupons."/><CouponManager coupons={coupons.map((x:any)=>({...x,value:Number(x.value),minOrder:Number(x.minOrder)}))}/></div>}
