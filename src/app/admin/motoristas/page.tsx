import{requirePermission}from"@/lib/access";import{getDrivers}from"@/lib/data";import{PageHeader}from"@/components/layout/page-header";import{Card}from"@/components/ui/card";import{Badge}from"@/components/ui/badge";import{Truck}from"lucide-react";
export default async function Page(){await requirePermission("routes.manage");const drivers=await getDrivers();return <div className="page-container"><PageHeader title="Motoristas" description="Equipe disponível para coletas e entregas."/><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{drivers.map((d:any)=><Card key={d.id} className="p-4"><div className="flex gap-3"><span className="grid size-10 place-items-center rounded-lg bg-blue-50 text-[var(--brand)]"><Truck className="size-5"/></span><div><div className="flex items-center gap-2"><strong>{d.name}</strong><Badge variant="success">Ativo</Badge></div><p className="mt-1 text-xs text-slate-500">{d.phone??"Sem telefone"}</p><p className="mt-1 text-xs text-slate-500">{d.staffProfile?.vehicle??"Veículo não informado"} · {d.staffProfile?.plate??"—"}</p></div></div></Card>)}</div></div>}
