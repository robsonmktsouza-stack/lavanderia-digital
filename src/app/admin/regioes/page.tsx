import{requirePermission}from"@/lib/access";import{getAdminRegions}from"@/lib/admin-data";import{PageHeader}from"@/components/layout/page-header";import{RegionManager}from"@/components/admin/region-manager";
export default async function Page(){await requirePermission("settings.manage");const regions=await getAdminRegions();return <div className="page-container"><PageHeader title="Regiões e taxas" description="Defina áreas atendidas, pedido mínimo e taxas de coleta e entrega."/><RegionManager regions={regions.map((x:any)=>({...x,collectionFee:Number(x.collectionFee),deliveryFee:Number(x.deliveryFee),minOrderValue:Number(x.minOrderValue)}))}/></div>}
