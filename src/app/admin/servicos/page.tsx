import { requirePermission } from "@/lib/access";import { getAdminServices } from "@/lib/admin-data";import { PageHeader } from "@/components/layout/page-header";import { ServiceManager } from "@/components/admin/service-manager";
export default async function Page(){await requirePermission("services.manage");const services=await getAdminServices();return <div className="page-container"><PageHeader title="Serviços e preços" description="Cadastre serviços, unidades, preços e prazos de processamento."/><ServiceManager services={services.map((x:any)=>({...x,price:Number(x.price)}))}/></div>}
