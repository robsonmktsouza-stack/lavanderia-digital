import{auth}from"@/auth";import{hasPermission}from"@/lib/access";import{prisma}from"@/lib/prisma";import{audit}from"@/lib/audit";import{z}from"zod";
const schema=z.object({code:z.string().min(3).optional(),description:z.string().nullable().optional(),type:z.enum(["PERCENT","FIXED"]).optional(),value:z.coerce.number().positive().optional(),minOrder:z.coerce.number().min(0).optional(),maxUses:z.coerce.number().int().positive().nullable().optional(),active:z.boolean().optional()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user||!hasPermission(s.user,"settings.manage"))return Response.json({error:"Sem permissão"},{status:403});const{id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return Response.json({error:p.error.issues[0]?.message??"Dados inválidos"},{status:400});if(!prisma)return Response.json({ok:true,demo:true});const data:any={...p.data};if(data.code)data.code=String(data.code).trim().toUpperCase();const row=await prisma.coupon.update({where:{id},data});await audit({actorId:s.user.id,action:"UPDATE",entity:"Coupon",entityId:id,metadata:data});return Response.json({ok:true,id:row.id})}
