import{auth}from"@/auth";import{hasPermission}from"@/lib/access";import{prisma}from"@/lib/prisma";import{audit}from"@/lib/audit";import{z}from"zod";
const schema=z.object({name:z.string().min(2).optional(),locationName:z.string().min(2).optional(),address:z.string().min(3).optional(),compartment:z.string().nullable().optional(),active:z.boolean().optional(),status:z.enum(["AVAILABLE","RESERVED","OCCUPIED","DISABLED"]).optional()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user||!hasPermission(s.user,"settings.manage"))return Response.json({error:"Sem permissão"},{status:403});const{id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return Response.json({error:p.error.issues[0]?.message??"Dados inválidos"},{status:400});if(!prisma)return Response.json({ok:true,demo:true});const row=await prisma.locker.update({where:{id},data:p.data});await audit({actorId:s.user.id,action:"UPDATE",entity:"Locker",entityId:id,metadata:p.data as any});return Response.json({ok:true,id:row.id})}
