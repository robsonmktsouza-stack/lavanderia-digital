import { auth } from "@/auth";import { prisma } from "@/lib/prisma";import { PageHeader } from "@/components/layout/page-header";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { ProfileForm } from "@/components/customer/profile-form";
export default async function Page(){const s=await auth();const dbUser=prisma?await prisma.user.findUnique({where:{id:s!.user.id},select:{name:true,email:true,phone:true,cpf:true}}):{name:s!.user.name??"Cliente",email:s!.user.email??"cliente@lavanderia.local",phone:"11988887777",cpf:""};return <div className="page-container"><PageHeader title="Minha conta" description="Dados de cadastro e segurança."/><Card className="max-w-2xl"><CardHeader><CardTitle>Dados pessoais</CardTitle></CardHeader><CardContent><ProfileForm user={dbUser!}/></CardContent></Card></div>}
