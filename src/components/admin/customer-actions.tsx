"use client";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";export function CustomerActions({id,status}:{id:string;status:string}){const r=useRouter();return <Button variant="outline" size="sm" onClick={async()=>{await fetch(`/api/admin/customers/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:status==="ACTIVE"?"DISABLED":"ACTIVE"})});r.refresh();}}>{status==="ACTIVE"?"Desativar":"Reativar"}</Button>}
