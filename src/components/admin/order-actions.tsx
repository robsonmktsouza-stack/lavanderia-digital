"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";
const statuses=["AWAITING_PAYMENT","CONFIRMED","COLLECTION_SCHEDULED","DRIVER_EN_ROUTE_COLLECTION","COLLECTED","RECEIVED_AT_LAUNDRY","IN_PROCESS","READY_FOR_DELIVERY","DELIVERY_SCHEDULED","DRIVER_EN_ROUTE_DELIVERY","DELIVERED","CANCELED"];
export function OrderActions({id,status,drivers}:{id:string;status:string;drivers:any[]}){const r=useRouter();const[st,setSt]=useState(status);const[driver,setDriver]=useState("");const[busy,setBusy]=useState(false);return <div className="flex flex-wrap gap-2"><select value={st} onChange={e=>setSt(e.target.value)} className="h-8 rounded-md border bg-white px-2 text-xs">{statuses.map(x=><option key={x} value={x}>{x}</option>)}</select><select value={driver} onChange={e=>setDriver(e.target.value)} className="h-8 rounded-md border bg-white px-2 text-xs"><option value="">Motorista...</option>{drivers.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><Button size="sm" variant="outline" disabled={busy} onClick={async()=>{setBusy(true);await fetch(`/api/admin/orders/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:st,driverId:driver||undefined})});setBusy(false);r.refresh();}}>{busy?"Salvando...":"Salvar"}</Button></div>}
