import Link from "next/link";import { CalendarDays, ChevronRight } from "lucide-react";import { Card } from "@/components/ui/card";import { StatusBadge } from "@/components/layout/status-badge";import { dateTime, money } from "@/lib/utils";
export function OrderCard({order}:{order:any}){return <Link href={`/app/pedidos/${order.id}`}><Card className="p-4 transition hover:border-slate-300 hover:shadow-sm"><div className="flex items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><strong className="text-sm">{order.number}</strong><StatusBadge status={order.status}/></div><div className="mt-3 flex items-center gap-2 text-xs text-slate-500"><CalendarDays className="size-3.5"/> Entrega: {dateTime(order.deliveryExpectedAt)}</div></div><div className="flex items-center gap-3"><strong>{money(order.total)}</strong><ChevronRight className="size-4 text-slate-400"/></div></div></Card></Link>}
