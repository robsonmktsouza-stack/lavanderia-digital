"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export function ProfileForm({user}:{user:{name:string;email:string;phone?:string|null;cpf?:string|null}}){const router=useRouter();const[msg,setMsg]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);return <form className="grid gap-4 sm:grid-cols-2" onSubmit={async e=>{e.preventDefault();setBusy(true);setMsg("");setError("");const fd=new FormData(e.currentTarget);const r=await fetch("/api/profile",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({name:fd.get("name"),phone:fd.get("phone"),cpf:fd.get("cpf")})});const d=await r.json();setBusy(false);if(!r.ok){setError(d.error??"Falha ao salvar");return;}setMsg("Dados atualizados.");router.refresh();}}><div className="space-y-1.5"><Label>Nome</Label><Input name="name" defaultValue={user.name} required/></div><div className="space-y-1.5"><Label>E-mail</Label><Input defaultValue={user.email} disabled/></div><div className="space-y-1.5"><Label>Telefone</Label><Input name="phone" defaultValue={user.phone??""} inputMode="tel"/></div><div className="space-y-1.5"><Label>CPF</Label><Input name="cpf" defaultValue={user.cpf??""} inputMode="numeric"/></div>{error&&<p className="sm:col-span-2 rounded-lg bg-red-50 p-3 text-xs text-red-700">{error}</p>}{msg&&<p className="sm:col-span-2 rounded-lg bg-emerald-50 p-3 text-xs text-emerald-700">{msg}</p>}<div className="sm:col-span-2"><Button disabled={busy}>{busy?"Salvando...":"Salvar alterações"}</Button></div></form>}
