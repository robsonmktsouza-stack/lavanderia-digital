"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
const F=({name,label,type="text",className="",required=true}:{name:string;label:string;type?:string;className?:string;required?:boolean})=><div className={`space-y-1.5 ${className}`}><Label>{label}</Label><Input name={name} type={type} required={required}/></div>;
export function RegisterForm(){const router=useRouter();const[error,setError]=useState("");const[loading,setLoading]=useState(false);return <form className="grid gap-4 sm:grid-cols-2" onSubmit={async e=>{e.preventDefault();setLoading(true);setError("");const fd=new FormData(e.currentTarget);const res=await fetch("/api/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(fd))});const data=await res.json();if(!res.ok){setError(data.error??"Não foi possível cadastrar.");setLoading(false);return}router.push("/entrar?cadastrado=1")}}><F name="name" label="Nome completo"/><F name="cpf" label="CPF"/><F name="phone" label="Telefone" type="tel"/><F name="email" label="E-mail" type="email"/><F name="password" label="Senha" type="password"/><F name="passwordConfirm" label="Confirmar senha" type="password"/><div className="sm:col-span-2 mt-2 border-t pt-4 text-xs font-semibold uppercase tracking-wide text-slate-400">Endereço principal</div><F name="postalCode" label="CEP"/><F name="street" label="Rua / avenida"/><F name="number" label="Número"/><F name="complement" label="Complemento" required={false}/><F name="district" label="Bairro"/><F name="city" label="Cidade"/><F name="state" label="UF"/>{error&&<p className="sm:col-span-2 rounded-lg bg-red-50 p-3 text-xs text-red-700">{error}</p>}<Button className="sm:col-span-2" disabled={loading}>{loading?"Criando...":"Criar minha conta"}</Button></form>}
